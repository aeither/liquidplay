import { myWorkflow } from './my-workflow';
import { transactionRewardsWorkflow } from './transactionRewards';
import { weatherWorkflow } from './weatherWorkflow';

// Export all workflows from this file
export const workflows = {
  weatherWorkflow,
  transactionRewardsWorkflow,
  myWorkflow
};